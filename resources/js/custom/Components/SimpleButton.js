import BaseButton from "../BaseClasses/BaseButton.js";

class SimpleButton extends BaseButton {
    constructor (id, type, valueName, disabled) {
        super (id, type, valueName, disabled);
    }
}
export default SimpleButton